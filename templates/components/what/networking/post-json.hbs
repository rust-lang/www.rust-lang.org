// This will POST a body of
//     `{"delete": true, "user": 42}`
#[derive(Serialize)]
struct Body {
    delete: bool,
    user: u32,
}

let client = reqwest::Client::new();
let res = client.post("http://example.org")
    .json(&Body {
        delete: true,
        user: 42,
    })
    .send()?;
